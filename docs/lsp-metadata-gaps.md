# LSP metadata gaps

Step 6.2's result: the Fusion LSP payload cannot populate the `ManifestCacheProjectAddedEvent` contract that `QueryManifestService` and every panel, tree, lens, and language provider consume. This records what is missing and the evidence behind it, so no LSP `ProjectMetadataSource` is written against a contract it cannot discharge. The manifest source (`src/metadata/manifestMetadataSource.ts`) remains the only `ProjectMetadataSource` implementation. Revisit only when Fusion exposes a contract-complete command payload.

## What the contract needs versus what three commands return

The three commands closest to project metadata are `dbt.getProjectInfo`, `dbt.listNodes`, and `dbt.getCurrentNode`, captured against Fusion 2.0.5 in [`docs/refactor/lsp-commands.md`](refactor/lsp-commands.md). None returns the eleven metadata maps and `modelDepthMap` the event carries.

| Contract need                                       | LSP command tried    | Captured result                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Macro metadata (`MacroMetaMap`)                     | none advertised      | No command inventories macros; not captured.                                                                                                                                                                                                                                                                                                         |
| Docs blocks (`DocMetaMap`)                          | none advertised      | No command returns doc block content; not captured.                                                                                                                                                                                                                                                                                                  |
| Exposures (`ExposureMetaMap`)                       | none advertised      | No command returns exposure metadata; not captured.                                                                                                                                                                                                                                                                                                  |
| Tests (`TestMetaMap`)                               | none advertised      | No command returns test node metadata; not captured.                                                                                                                                                                                                                                                                                                 |
| Unit tests (`UnitTestMetaMap`)                      | none advertised      | Not captured; no command in the S2 trace targeted unit tests.                                                                                                                                                                                                                                                                                        |
| Sources (`SourceMetaMap`)                           | none advertised      | Not captured; no command in the S2 trace targeted sources.                                                                                                                                                                                                                                                                                           |
| Functions (`FunctionMetaMap`)                       | none advertised      | Not captured; no command in the S2 trace targeted functions.                                                                                                                                                                                                                                                                                         |
| Metrics (`MetricMetaMap`)                           | none advertised      | No command returns metric definitions; not captured.                                                                                                                                                                                                                                                                                                 |
| Semantic models (`SemanticModelMetaMap`)            | none advertised      | No command returns semantic model metadata; not captured.                                                                                                                                                                                                                                                                                            |
| Model depth inputs (`modelDepthMap`)                | `dbt.listNodes`      | `dbt.listNodes` is the only candidate source for the dependency graph depth is computed over, and it returned `null` on the fixture project (see the node/graph row below); depth cannot be sourced from the LSP on this capture.                                                                                                                    |
| Node/graph metadata (`NodeMetaMap`, `GraphMetaMap`) | `dbt.listNodes`      | Returned `null` on a project containing a broken `ref`, after its "Computing Lineage" progress ended. Does not confirm the response shape on a clean project.                                                                                                                                                                                        |
| Project-level counts                                | `dbt.getProjectInfo` | Returned `{"adapter_type":"snowflake","models_count":0,"models_count_is_estimate":true,"project_name":"single_project"}` before parsing completed, and `models_count: 2, models_count_is_estimate: false` after, on an earlier two-model run. This is a count and an adapter name, not the node/macro/test/exposure/metric detail the event carries. |
| Column metadata for a node                          | `dbt.getCurrentNode` | Returned `null` for the one position tried; with no arguments it produced no response within 60 seconds on a client that does not answer progress.                                                                                                                                                                                                   |

## Why no LSP source is written

A `ProjectMetadataSource` that can never publish a complete event is dead code — it would either omit required fields or `refresh()` into permanent failure. Because the commands available on Fusion 2.0.5 supply at most a project name, an adapter type, and an unreliable model count, no LSP-backed implementation exists. `FusionProjectIntegration` continues to parse `manifest.json` locally and publish through `ManifestMetadataSource`, per plan step 6.2 and [`docs/architecture.md`](architecture.md#the-metadata-port).

## Revisiting this gap

Re-run the capture in `docs/refactor/lsp-commands.md` against a newer Fusion release before writing an LSP `ProjectMetadataSource`. A contract-complete source needs, at minimum, a command that returns full node metadata (not just counts) on a clean project, plus commands or an equivalent for macros, docs, exposures, tests, metrics, and semantic models. Until then, splitting the event into partially-LSP-backed and partially-manifest-backed fields is out of scope — `QueryManifestService` has one producer per project, not a merge of two.
