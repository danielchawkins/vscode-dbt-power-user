---
name: <directory-name>
description: <What it does.> Use when <trigger>, <trigger>, or <trigger>.
# license: <name, or a bundled license filename>
# compatibility: <environment requirement — only if there is a real one>
# disable-model-invocation: true   # Claude Code + Cursor only; Codex needs agents/openai.yaml
---

# <Skill name>

<One or two sentences: what this skill is for, and what it deliberately does not cover.>

## When to use

- <Concrete trigger, in the words a caller would type.>
- Not for <adjacent case> — that belongs in <where>.

## Procedure

1. <Imperative step. Name the input and the observable result.>
2. <...>
3. <Verification: the command or check that proves it worked.>

Done when <observable condition>.

## Gotchas

- [Non-obvious local fact the agent gets wrong without being told.]

## References

- `references/<file>.md` — read when <condition>.

<!-- Delete every heading you do not need. A short skill is not an unfinished one. -->
